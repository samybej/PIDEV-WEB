{% extends 'back.html.twig' %}
{% block body %}

    <section class="content">
        <div class="row">
            <div class="col-md-6">
                <div class="box box-primary">
                    <div class="box-header">
                        <h3 class="box-title">Ajouter véhicule dans le garage</h3>
                    </div><!-- /.box-header -->
                    <!-- form start -->


                    {{ form_start(f) }}
                    <div class="box-body">
                        <div class="form-group">
                            <label for="exampleInputPassword1">Immatriculation</label>
                            {{ form_widget(f.vehicule , { 'attr': { 'class':'form-control', 'placeholder': 'Immatriculation'}}) }}
                        </div>
                        <div class="form-group">
                            <label for="exampleInputPassword1">Etat</label>
                            {{ form_widget(f.etat , { 'attr': { 'class':'form-control','placeholder': 'Etat'}}) }}

                        </div>
                    </div>
                    <!-- /.box-body -->
                    <div class="box-footer">
                        {{ form_widget (f.Ajouter) }}
                    </div>

                    {{ form_end(f) }}
                </div><!-- /.box -->
            </div>
        </div>
    </section>



{% endblock %}